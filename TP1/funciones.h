
/** \brief suma dos numeros del tipo flotantes
 *
 * \param float primer numero ingresado
 * \param float segundo numero ingresado
 * \return float retorna la suma de los numeros ingresados por el usuario
 *
 */
float funcionSuma(float, float);
/** \brief realiza una resta entre los valores ingresados por el usuario
 *
 * \param float minuendo
 * \param float sustraendo
 * \return float retorna la resta del primer numero disminuido por el segundo numero
 *
 */
float funcionResta(float, float);
/** \brief realiza una division entre dos numeros flotantes
 *
 * \param float numero dividendo
 * \param float numero divisor
 * \return float retorna el cociente de ambos numeros
 *
 */
float funcionDivision(float, float);
/** \brief realiza una multiplicacion entre dos numeros flotantes
 *
 * \param float numero multiplicando
 * \param float numero multiplicador
 * \return float retorna el producto de ambos numeros
 *
 */
float funcionMultiplicacion(float, float);

/** \brief factoriza el numero ingresado por el usuario
 *
 * \param numero a factorizar
 * \return retorna el numero factorizado
 *
 */
int funcionFactorial (float);
